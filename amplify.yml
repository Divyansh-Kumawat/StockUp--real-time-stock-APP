version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - node -v
        - npm -v
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
      - ../.env.production
      - ../next.config.ts
      - ../package.json
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*